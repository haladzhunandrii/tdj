# Production API URL (set at build time: vite build)
# VITE_API_BASE_URL=https://your-api.onrender.com

# Vite dev server proxy target (Docker only — not used by the browser)
# VITE_PROXY_TARGET=http://backend:8000
